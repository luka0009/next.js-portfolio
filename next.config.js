/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "www.pngmart.com",
      "user-images.githubusercontent.com",
      "files.raycast.com",
    ],
  },
};

module.exports = nextConfig;
